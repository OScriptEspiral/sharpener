Listing all exercises topics
Lists all existing topics.
---
tags:
  - Exercise
  - Topic
parameters:
  - name: page
    in: query
    required: false
    type: integer
    minimum: 0
    default: 0
    description: The number of pages to skip before starting to collect the result set.
  - name: page_size
    in: query
    name: page_size
    required: false
    type: integer
    minimum: 1
    default: 100
    description: The number of topics to return.
responses:
  200:
    description: A list of topics
    schema:
      type: array
      items:
        type: string
        description: A topic
        example: math
